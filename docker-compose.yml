version: '3'
services:

  bloggle-db:
    container_name: bloggle-db
    build: ./services/bloggle-db/
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=bloggle
      - POSTGRES_PASSWORD=bloggle
